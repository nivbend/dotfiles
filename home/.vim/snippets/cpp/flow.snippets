snippet fori
	for (int ${1:i} = 0; $1 < ${2:count}; ${3:++$1})
	{
		${0}
	}
snippet fore
	for (${1:auto} ${2:item} : ${3:container})
	{
		${0}
	}
snippet iter
	for (auto ${1:i} = ${2:container}.begin(); $1 != $2.end(); ++$1)
	{
		${0}
	}
